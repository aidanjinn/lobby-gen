"use client";
import { useState } from "react";import{Check,Copy}from"lucide-react";
export function CopyCode({code}:{code:string}){const[state,setState]=useState<"idle"|"copied"|"error">("idle");async function copy(){try{if(navigator.clipboard&&window.isSecureContext)await navigator.clipboard.writeText(code);else{const area=document.createElement("textarea");area.value=code;area.style.position="fixed";area.style.opacity="0";document.body.appendChild(area);area.select();if(!document.execCommand("copy"))throw new Error();area.remove()}setState("copied");setTimeout(()=>setState("idle"),1800)}catch{setState("error")}}return <button type="button" className="button ghost" onClick={copy}>{state==="copied"?<Check size={17}/>:<Copy size={17}/>} {state==="copied"?"Copied!":state==="error"?"Copy failed":code}</button>}
